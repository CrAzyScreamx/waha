- Read `AGENTS.md` for more instructions

## Project Vault

Before looking for project context in source files, check the vault at
`./waha/`. Start with `tools.md` for an overview, then navigate to the relevant
`<type>/<feature>.md`. The vault is the fast path — use it before digging into
source.
